describe('DeviceBookingService', function() {

  beforeEach(angular.mock.module(require('./index').name))

  it('should ...', inject(function(DeviceBookingService) {

	//expect(DeviceBookingService.doSomething()).toEqual('something')

  }))

})
